[project]
name = "launchpad-skill"
version = "0.1.0"
description = "Launchpad Skill - Agentic Company Management CLI"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
ls = "launchpad_skill.cli:main"
launchpad-skill = "launchpad_skill.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
